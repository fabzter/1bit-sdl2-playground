#pragma once

/**
 * @struct Vec2f
 * @brief A simple structure to hold 2D float coordinates.
 */
struct Vec2f {
    float x = 0.0f;
    float y = 0.0f;
};

/**
 * @struct Vec2i
 * @brief A simple structure to hold 2D integer coordinates.
 */
struct Vec2i { int x = 0; int y = 0; };